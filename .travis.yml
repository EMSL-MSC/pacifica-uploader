language: python
python:
  - "2.7"
install: "pip install -r requirements.txt"
script:
 - bash -xe pylint-everything.sh
 - coverage run --include='ingest*' ingest_unit_tests.py -v
 - codeclimate-test-reporter
